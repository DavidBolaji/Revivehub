# GitHub API
GITHUB_TOKEN=your_github_personal_access_token
GITHUB_API_URL=https://api.github.com

# Claude API (Anthropic)
ANTHROPIC_API_KEY=your_anthropic_api_key
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# OpenAI API (optional alternative)
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gpt-4

# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# Database (if needed)
DATABASE_URL=your_database_connection_string

# ============================================
# Redis Cache Configuration (Optional)
# ============================================
# Redis is used for caching GitHub API responses in production
# For development, an in-memory cache is used by default
# 
# Option 1: Standard Redis
# REDIS_URL=redis://localhost:6379
#
# Option 2: Upstash Redis (Serverless)
# Get credentials from: https://console.upstash.com/
UPSTASH_REDIS_REST_URL=your_upstash_redis_rest_url
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token

# ============================================
# NextAuth Configuration
# ============================================
# NEXTAUTH_URL: The canonical URL of your site (use http://localhost:3000 for local development)
# In production, set this to your actual domain (e.g., https://yourdomain.com)
NEXTAUTH_URL=http://localhost:3000

# NEXTAUTH_SECRET: A random string used to encrypt tokens and sign cookies
# IMPORTANT: Generate a secure secret using one of these methods:
#   - OpenSSL: openssl rand -base64 32
#   - Node.js: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#   - Online: https://generate-secret.vercel.app/32
# Keep this secret secure and never commit it to version control!
NEXTAUTH_SECRET=your_nextauth_secret

# ============================================
# GitHub OAuth Configuration
# ============================================
# To obtain GitHub OAuth credentials:
# 1. Go to GitHub Settings: https://github.com/settings/developers
# 2. Click "OAuth Apps" in the left sidebar
# 3. Click "New OAuth App" button
# 4. Fill in the application details:
#    - Application name: ReviveHub (or your preferred name)
#    - Homepage URL: http://localhost:3000 (or your production URL)
#    - Authorization callback URL: http://localhost:3000/api/auth/callback/github
#      (In production: https://yourdomain.com/api/auth/callback/github)
# 5. Click "Register application"
# 6. Copy the "Client ID" to GITHUB_ID below
# 7. Click "Generate a new client secret" and copy it to GITHUB_SECRET below
#
# IMPORTANT: The callback URL must match exactly, including the protocol (http/https)
# For production, create a separate OAuth App with your production domain

# GitHub OAuth App Client ID
GITHUB_ID=your_github_oauth_app_client_id

# GitHub OAuth App Client Secret
GITHUB_SECRET=your_github_oauth_app_client_secret

# ============================================
# Scanner Configuration
# ============================================
# SCANNER_TIMEOUT_MS: Maximum time in milliseconds for a complete repository scan
# Default: 30000 (30 seconds)
SCANNER_TIMEOUT_MS=30000

# SCANNER_MAX_FILE_SIZE_MB: Maximum file size in megabytes to process
# Files larger than this will be skipped to prevent memory issues
# Default: 1 (1 MB)
SCANNER_MAX_FILE_SIZE_MB=1

# SCANNER_CACHE_TTL_SECONDS: Time-to-live for cached scan results in seconds
# Cached results are invalidated when repository commit SHA changes
# Default: 600 (10 minutes)
SCANNER_CACHE_TTL_SECONDS=600
